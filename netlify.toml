[build]
  base = ""
  publish = "apps/web/dist"
  command = "npm run build --workspace=@abhiyantrix/web"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
